spring:
  application:
    name: ai-test
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/aitest}

server:
  port: 8080

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}
